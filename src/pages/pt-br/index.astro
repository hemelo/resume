---
import Layout from "@/layouts/Layout.astro";

import cv from "@cv-br";
import shared from "@shared-cv";
import { type CV } from "@types";

const { name, label } = cv.basics

const typedCv = cv as CV;

---

<Layout title={`${name} Resume - ${label}`} shared={shared} cv={typedCv}></Layout>
